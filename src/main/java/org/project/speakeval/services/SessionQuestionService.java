package org.project.speakeval.services;

import org.project.speakeval.domain.SessionQuestion;

public interface SessionQuestionService {
    SessionQuestion createSessionQuestion(SessionQuestion sessionQuestion);
}
